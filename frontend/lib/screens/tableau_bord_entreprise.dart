import 'package:flutter/material.dart';

class TableauBordEntreprise extends StatelessWidget {

  @override
  Widget build(BuildContext contex){
    return Scaffold(
      appBar: AppBar(
        title: Text("je suis un Text du niveau 1"),
      ),
    );
    
  }
}